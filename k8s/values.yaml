app:
  project: fiapx
  name: api
  version: 1.0.0
  env: dev
  port: 5000
  routePrefix: api

service:
  minReplicas: 2
  maxReplicas: 4

image:
  repository: ""
  tag: latest

aws:
  region: us-east-1

auth:
  enabled: true
  authority: "https://dev-oq5eot5o33s27xpo.us.auth0.com/"
  audience: ""

storage:
  bucketName: fiapx-dev-media
  uploadUrlExpirationMinutes: 15
  downloadUrlExpirationMinutes: 5
  publicServiceUrl: ""

tables:
  processingJobs: fiapx-dev-videos-db

queueNames:
  VideoProcessingRequested: video-processing-requested

secrets:
  clusterSecretStore: fiapx-aws-secrets
