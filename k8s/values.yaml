app:
  project: fiapx
  name: api
  version: 1.0.0
  env: dev
  port: 5000
  routePrefix: api

service:
  minReplicas: 2
  maxReplicas: 4

image:
  repository: ""
  tag: latest

aws:
  region: us-east-1

auth:
  enabled: true
  userInfoEndpoint: https://dev-oq5eot5o33s27xpo.us.auth0.com/userinfo

storage:
  bucketName: "fiapx-dev-web-499509007072"
  uploadUrlExpirationMinutes: 15
  downloadUrlExpirationMinutes: 5

tables:
  processingJobs: ""

queueNames:
  VideoProcessingRequested: video-processing-requested

datadog:
  otlpEndpoint: "https://otlp.us5.datadoghq.com"

secrets:
  clusterSecretStore: fiapx-aws-secrets
